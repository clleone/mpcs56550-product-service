apiVersion: v1
kind: Service
metadata:
  name: product-service
  namespace: staging
spec:
  selector:
    app: product-service
  type: NodePort
  ports:
    - port: 5001
      targetPort: 5001
      nodePort: 30011